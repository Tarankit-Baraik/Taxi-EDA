# Core data handling
pandas>=2.2,<3.0
numpy>=1.26,<2.0
pyarrow>=15.0            # data_loader.load_month reads TLC parquet files directly

# HTTP + local data acquisition (data_loader.py)
requests>=2.31

# Statistics (scipy.stats -- notebooks 02-06: Kruskal-Wallis, Mann-Whitney, chi2, Spearman/Pearson)
scipy>=1.12,<2.0

# Geospatial (data_loader.load_zone_shapes; notebooks 02, 03, 05)
geopandas>=0.14,<1.0
shapely>=2.0              # geopandas dependency, pinned explicitly for geometry ops
mapclassify>=2.6          # scheme="quantiles" choropleth classification

# Visualization (visualization.py)
matplotlib>=3.8,<4.0
seaborn>=0.13              # plot_heatmap, plot_correlation_matrix, plot_boxplot styling
folium>=0.16               # interactive heatmaps and OD flow maps (notebooks 03, 05)

# Notebook execution
jupyter>=1.0
notebook>=7.0

# Python >=3.9 required (preprocessing.py / monthly_analysis.py use PEP 585
# built-in generics: tuple[pd.DataFrame, dict], list[str])
